{
  "name": "ethereum-offline-tx",
  "version": "1.0.0",
  "description": "Command-line interface to send transactions from an offline ethereum wallet",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "beskay",
  "license": "ISC",
  "dependencies": {
    "ethers": "^5.7.0"
  }
}
